Produce, using the Java Compiler-to-Compiler, an elementary Parser that recognizes any arithmetic expression with balanced parentheses, including the operations +, -, *, / , ^ and Square Root (#) having any real numbers as operands . Ensure unambiguity and correct precedence, with production rules in EBNF format. Every arithmetic expression must present its respective numerical result as output.